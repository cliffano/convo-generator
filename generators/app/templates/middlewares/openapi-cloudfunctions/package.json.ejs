{
  "name": "<%= config.id() %>",
  "version": "<%= config.convoSpec.info.version %>",
  "description": "<%= config.convoSpec.info.description %>",
  "main": "index.js",
  "author": "<%= config.convoSpec.info.contact.name %> <<%= config.convoSpec.info.contact.email %>> (<%= config.convoSpec.info.contact.url %>)",
  "license": "<%= config.convoSpec.info.license.name %>",
  "dependencies": {
    <%
      const dependencies = config.convoSpec.middlewares.openapi_cloudfunctions.dependencies;
      for (let i = 0, len = dependencies.length; i < len; i++) {
    %>
    "<%= dependencies[i].name %>": "<%= dependencies[i].version %>",
    <%
      }
    %>
    "convo-node": "^0.0.2"
  },
  "devDependencies": {
    "serverless-google-cloudfunctions": "^1.2.0"
  }
}
